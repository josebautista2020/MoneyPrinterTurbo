# Content Studio Extensions

Reusable Content Studio AI capabilities live here. Extensions must remain vertical-agnostic and must not import from `verticals/`.

MoneyPrinterTurbo-specific integration is isolated behind `extensions/mpt_adapter/`.
